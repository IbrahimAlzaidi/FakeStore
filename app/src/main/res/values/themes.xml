<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FakeStore" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FakeStore" parent="Base.Theme.FakeStore" />

    <style name="ShapeAppearanceOverlay.TextInputLayout.Rounded" parent="">
        <item name="cornerSize">20dp</item>
    </style>

    <style name="CardImageStyle" parent="">
        <item name="android:layout_marginStart">@dimen/spacing_medium</item>
        <item name="android:layout_marginEnd">@dimen/spacing_medium</item>
        <item name="android:layout_marginTop">@dimen/spacing_medium</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:backgroundTint">@color/card</item>
        <item name="android:background">@color/card</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.Material3.Corner.Large</item>
    </style>

    <style name="rounded_corner" parent="">
        <item name="cornerSize">20dp</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.RoundedCorner</item>
    </style>

    <style name="ShapeAppearanceOverlay.RoundedCorner" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

    <style name="RoundedImageViewStyle" parent="">
        <item name="android:layout_marginStart">@dimen/spacing_medium</item>
        <item name="android:layout_marginEnd">@dimen/spacing_medium</item>
        <item name="android:layout_marginTop">@dimen/spacing_medium</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:backgroundTint">@color/card</item>
        <item name="android:background">@color/card</item>
        <item name="shapeAppearance">@style/ShapeAppearanceOverlay.RoundedCorner</item>
    </style>

    <style name="normalTextStyle.input" parent="">
        <item name="android:textColor">#99000000</item>
        <item name="android:background">@drawable/background_edit_text</item>
        <item name="android:padding">16dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

</resources>